{
  "name": "wavtrack-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 